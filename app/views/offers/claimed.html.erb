<%= button_to "Continue Discovering", discover_path, method: :get, class: "link-out" %>

<%= react_component("Discover", props: {
  offers: @claimed_offers, 
  tags: @tags, 
  player: @player, 
  renderForm: false,
  actionSet: 'unclaim'
}, prerender: false) %>